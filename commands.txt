# Docker

# TO run in prod
docker build -t latte-frontend .
docker run -p 3000:80 latte-frontend


# To run in dev
docker-compose -f docker-compose-dev.yml build --no-cache
docker-compose -f docker-compose-dev.yml up
